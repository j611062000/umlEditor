package src.interactiveComponents.buttons;

import src.configuration.configOfMode.ConfigOfMode;

public class GeneralizationLineButton extends SquareButton {

    public GeneralizationLineButton() {
        super(ConfigOfMode.NAME_OF_GENERALIZATION_LINE_MODE);
    }
    
}
